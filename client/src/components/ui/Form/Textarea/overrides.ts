import {TextareaProps} from 'baseui/textarea';

export const overrides = {
  Root: {
    style: {
      borderTopWidth: '1px',
      borderBottomWidth: '1px',
      borderLeftWidth: '1px',
      borderRightWidth: '1px',
    },
  },
} as TextareaProps['overrides'];
